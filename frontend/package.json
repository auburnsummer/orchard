{
  "private": true,
  "name": "frontend",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "NODE_ENV=production preact build && mv ./build/index.html ./build/200.html",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^2.3.0",
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "lodash": "^4.17.19",
    "preact": "^10.1.0",
    "preact-cli-postcss": "^1.1.1",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^3.2.1",
    "tailwindcss": "^1.6.0",
    "tailwindcss-gap": "^6.0.0"
  }
}
